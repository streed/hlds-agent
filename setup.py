#!/usr/bin/env python

from distutils.core import setup

setup(name='hlds-agent',
      version='0.1',
      description='Where should I go do X?',
      author='Sean Reed',
      author_email='sean@reed.pub',
      packages=['hlds_agent'],)
